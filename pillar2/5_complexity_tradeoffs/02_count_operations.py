"""
Drill 02 - Count Operations

Write counted_linear_search(items, target) -> tuple[int, int]. Return the found
index (or -1) and the number of equality comparisons performed. Stop at the
first match. Empty input performs zero comparisons.

Use examples where the target is first, last, and absent. Explain how those
counts correspond to best- and worst-case O-notation.

Complexity check:
State the worst-case time and extra-space Big-O. In one sentence, explain the
main cost and why your chosen data structure or pattern fits the problem.

Thinking goal: connect an abstract growth rate to concrete executed operations.
"""


def counted_linear_search(items: list[int], target: int) -> tuple[int, int]:
    pass
