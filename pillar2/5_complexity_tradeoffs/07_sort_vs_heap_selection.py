"""
Drill 07 - Full Sort versus Heap Selection

Implement largest_sorted(numbers, k) using sorted and largest_heap(numbers, k)
using heapq.nlargest. Both return values descending and handle k <= 0 with [].
Do not mutate numbers.

State when O(n log n) sorting may be simpler and when O(n log k) selection is
preferable. Verify both implementations agree on duplicates.

Complexity check:
State the worst-case time and extra-space Big-O. In one sentence, explain the
main cost and why your chosen data structure or pattern fits the problem.

Thinking goal: select an algorithm from n, k, clarity, and output requirements.
"""


def largest_sorted(numbers: list[int], k: int) -> list[int]:
    pass


def largest_heap(numbers: list[int], k: int) -> list[int]:
    pass
