"""
Drill 03 - Unique Tags

Write unique_normalized_tags(tags) -> set[str]. Strip surrounding whitespace,
lowercase every nonempty tag, and return each resulting tag once.

Example: [" Python ", "python", "ETL", ""] becomes {"python", "etl"}.
Do not promise an output order.

Complexity check:
State the worst-case time and extra-space Big-O. In one sentence, explain the
main cost and why your chosen data structure or pattern fits the problem.

Thinking goal: choose a set when uniqueness matters and order does not.
"""


def unique_normalized_tags(tags: list[str]) -> set[str]:
    pass
