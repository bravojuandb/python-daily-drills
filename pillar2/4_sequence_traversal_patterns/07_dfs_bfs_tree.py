"""
Drill 07 - DFS and BFS Tree Traversal

A tree maps each node to an ordered list of children. Write depth_first(tree,
start) and breadth_first(tree, start), each returning visited node names. Use a
stack for iterative DFS and deque for BFS. Preserve child order in both outputs.

Example: A -> [B, C], B -> [D] gives DFS [A, B, D, C], BFS [A, B, C, D].

Complexity check:
State the worst-case time and extra-space Big-O. In one sentence, explain the
main cost and why your chosen data structure or pattern fits the problem.

Thinking goal: the frontier structure determines traversal order.
"""


def depth_first(tree: dict[str, list[str]], start: str) -> list[str]:
    pass


def breadth_first(tree: dict[str, list[str]], start: str) -> list[str]:
    pass
