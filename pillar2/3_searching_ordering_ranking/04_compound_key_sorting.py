"""
Drill 04 - Compound-Key Sorting

Write sort_scores(scores) returning (name, score) pairs ordered by score
descending, then name ascending case-insensitively. Do not mutate the dictionary.

Example: {"Zoe": 8, "amy": 10, "Bob": 10} becomes
[("amy", 10), ("Bob", 10), ("Zoe", 8)].

Complexity check:
State the worst-case time and extra-space Big-O. In one sentence, explain the
main cost and why your chosen data structure or pattern fits the problem.

Thinking goal: encode every tie rule in one deterministic sort key.
"""


def sort_scores(scores: dict[str, int]) -> list[tuple[str, int]]:
    pass
