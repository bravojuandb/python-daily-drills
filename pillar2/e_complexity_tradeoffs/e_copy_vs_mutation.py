"""
Drill 05 - Copy versus Mutation

Write append_copied(items, value), which leaves items unchanged, and
append_in_place(items, value), which mutates and returns the same list object.
Use `is` in examples to verify identity behavior.

State the time and auxiliary-space costs of both approaches for n existing items.

Complexity check:
State the worst-case time and extra-space Big-O. In one sentence, explain the
main cost and why your chosen data structure or pattern fits the problem.

Thinking goal: API semantics, runtime, and memory can all change together.
"""


def append_copied(items: list[int], value: int) -> list[int]:
    pass


def append_in_place(items: list[int], value: int) -> list[int]:
    pass
