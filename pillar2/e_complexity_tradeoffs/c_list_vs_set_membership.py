"""
Drill 03 - List versus Set Membership

Write find_known_ids(queries, known_ids) -> list[int]. Return queries present in
known_ids, preserving query order and duplicates. Convert known_ids to one set
before scanning queries; do not rebuild it inside the loop.

State the time and auxiliary-space complexity using q queries and n known IDs,
then compare it with testing membership directly against the list.

Complexity check:
State the worst-case time and extra-space Big-O. In one sentence, explain the
main cost and why your chosen data structure or pattern fits the problem.

Thinking goal: trade O(n) setup and memory for repeated average O(1) lookup.
"""


def find_known_ids(queries: list[int], known_ids: list[int]) -> list[int]:
    pass
