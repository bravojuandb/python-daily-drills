"""
Drill 04 - Variable-Size Sliding Window

Given positive integers, write shortest_subarray_at_least(numbers, target) -> int.
Return the minimum length of a contiguous subarray whose sum is at least target,
or 0 if none exists. Expand right and shrink left while the condition holds.

Target complexity: O(n) time and O(1) auxiliary space.

Complexity check:
State the worst-case time and extra-space Big-O. In one sentence, explain the
main cost and why your chosen data structure or pattern fits the problem.

Thinking goal: maintain the smallest valid window without revisiting positions.
"""


def shortest_subarray_at_least(numbers: list[int], target: int) -> int:
    pass
