"""
Drill 06 - Balanced Brackets

Write is_balanced(text) -> bool for (), [], and {}. Ignore non-bracket
characters. Use a list as a stack and fail early on an unmatched closing
bracket. An empty string is balanced.

Target complexity: O(n) time and O(n) worst-case auxiliary space.

Complexity check:
State the worst-case time and extra-space Big-O. In one sentence, explain the
main cost and why your chosen data structure or pattern fits the problem.

Thinking goal: the stack contains exactly the openings awaiting a matching close.
"""


def is_balanced(text: str) -> bool:
    pass
