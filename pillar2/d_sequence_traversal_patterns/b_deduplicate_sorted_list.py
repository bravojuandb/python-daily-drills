"""
Drill 02 - Deduplicate a Sorted List

Write compact_sorted(numbers) -> int. Modify the list in place so its first k
positions contain each distinct value once and return k. Values after k do not
matter. Use two pointers and no set or second list.

Example: [1, 1, 2, 2, 5] becomes [1, 2, 5, ...] and returns 3.

Complexity check:
State the worst-case time and extra-space Big-O. In one sentence, explain the
main cost and why your chosen data structure or pattern fits the problem.

Thinking goal: separate the read position from the next write position.
"""


def compact_sorted(numbers: list[int]) -> int:
    pass
