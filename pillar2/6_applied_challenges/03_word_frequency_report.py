"""
Drill 03 - Word-Frequency Report

Write word_frequency_report(text, limit=10) returning (word, count) pairs.
Lowercase text, treat maximal alphanumeric runs as words, count them, then rank
by count descending and word ascending. Return [] when limit <= 0.

Example: "Data, data! Pipelines." gives [("data", 2), ("pipelines", 1)].

Complexity check:
State the worst-case time and extra-space Big-O. In one sentence, explain the
main cost and why your chosen data structure or pattern fits the problem.

Thinking goal: build a deterministic parse, count, and top-results pipeline.
"""


def word_frequency_report(text: str, limit: int = 10) -> list[tuple[str, int]]:
    pass
