"""
Drill 11 - Generator Pipeline

Write a function:
    total_long_word_lengths(words: list[str]) -> int

Requirements:
1. Use a generator expression, not a list comprehension.
2. Consider only words with length at least `5`.
3. Sum the lengths of those words with `sum()`.
4. Return the final total.

Example:
>>> total_long_word_lengths(["sun", "planet", "asteroid", "sky", "galaxy"])
21

Thinking goal:
This drill is about building a small lazy pipeline and finishing with one focused built-in.
"""
